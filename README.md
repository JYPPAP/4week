## 21.11.18
해야 할 일
1. 수수료 계산과 계산기 분리하기
  - 중복입력
2. 수수료 계산에서 값을 입력할 때 3자리마다 ","가 추가될 수 있도록 만들기.
  - input 값에 적용
  - 적용된 값에서 다시 ","는 빼서 계산할 수 있도록 만들기
## 21.11.12
제작.
1. 수수료 계산 부분과 계산기 부분은 따로 동작한다.
2. 수수료 계산
  - 판매금액에 값을 입력할 때, ","가 붙으면서 입력이 되어야 한다.
    - input 태그로 바꿔야 함.
  - 금액을 입력하면 해당 값의 결과(수수료, 예상 적립금액)을 출력할 수 있게 만들면 된다.
  - 값이 "원" 이기 때문에 3자리마다 , 를 추가해줘야 한다.
  - 입력의 경우 숫자만 입력될 수 있는 방법 -> 예전에 봤던 것을 추가해주며, 입력부분을 input 태그로 변경하기.
    - 읽어온 값이 숫자면 내용을 출력하며 "뒤에서부터" 3번째 자리마다 ,를 삽입한다.
    - 3개 다 뒤에 "원"을 추가하면 좋을 것 같다.
    - 계산의 경우 기존의 것에서 약간만 수정을 하면 될 것 같다.
    * 값을 input 태그에 입력하면서 ,가 추가되어야 하는건가?
      - 그렇다고 한다면 ,를 제거하고 나서 값을 number로 받아오면 될 것 같다.
    - 숫자만 입력되게 할 것인지, 아니면 입력되어도 숫자가 아니면 아무것도 반환하지 않는것으로 할 것인지

3. 계산기
  - 기능 동작의 경우는 윈도우 계산기를 참고한다.
    - 윈도우 계산기의 경우는 숫자 입력 > 연산자 입력 > 숫자 입력 > 연산자 다시 입력시 앞의 값들이 계산 후 연산자 추가.
    - 초기값으로 0이 있음. 그래서 연산자를 추가해도 0+ 혹은 0- 등으로 출력됨.
    - calcFlag를 추가해서 연산자가 입력된 후 숫자가 입력되고 또 다시 연산자가 입력되었을 때 값을 
  - 멀티 계산이 동작하지 않아도 되며, 직접 사칙연산의 계산이 동작할 수 있으면 된다.
    그렇다면 숫자가 입력되고, 연산자가 입력되고(1개만), 다시 숫자가 입력되고 새로 연산자가 입력될 때 결과값을 출력하고 마지막에 입력한 연산자를 다시 출력하는 방법?
  - 연산자별로 분리? 해당 값이 입력되었을 때를 나누기.
  - 연산자가 가장 처음 입력되면 앞에 0을 추가
  - 값을 배열에 1개씩 저장. 숫자인지 연산자인지 체크, 연산자일 경우 해당 인덱스 값을 저장.
    - 인덱스 값 앞쪽은 숫자일테니 0번 ~ 연산자의 인덱스 값-1 까지는 숫자이므로 해당 값을 
  * 숫자와 연산자를 분리하고 계산하는 방법에 대해서
  1. 숫자와 연산자를 분리
  2. 숫자 + 연산자 + 숫자 후 연산자가 입력될 때 앞쪽에 있는 숫자 + 연산자 + 숫자를 계산하기.
  3. 앞의 값에 마지막으로 입력했던 연산자를 추가 후 새로운 값 기다리기.
  4. = 기호가 입력되면 
  * 처음에 0이 입력되어 있고 값을 A 연산자 B의 형태로 계속 분할.
  - 처음 값이 입력되면 0 + 입력한 숫자(연산자 앞부분 까지)를 분리 0은 A에 저장 +는 초기 연산자로 활용 
  - 입력한 숫자는 B에 저장 후 값을 계산한 다음 A에 모든 값 저장 후 A를 출력.
  - 연산자를 입력하고 숫자를 입력받으면 A에 연산자 B의 형태로 만들기.
  - 계산의 경우 ``으로 감싼다음 ${A} + ${연산자} + ${B}의 형태로 만들기
  * 연산자가 연속으로 입력될 경우만 바꿔주고, 숫자 다음 연산자가 올 경우만 값을 계산해주기
  초기값으로 0을 놓고 연산자는 기본값으로 +로 해 놓은 상태.
  연산자를 바꾼다면 0-, 0/, 0*의 형태가 된다?
  구조를 연산자 입력 후 숫자가 입력되는 구조를 반복하게한다면... 어떨까...?
  * 아니면 멀티계산. 숫자가 아닌것을 파싱 후 곱셈, 나눗셈, 덧셈, 뺄셈의 순서대로 계산 후 결과값 출력.
  - 그렇다면 숫자가 아닌것을 파싱하는 방법은 뭐가 있을까?
  1. 사칙연산 계산식(입력받은 값)에서 숫자를 연산자가 있는 부분마다 전부 잘라서 배열에 저장하기.
    ex) 12+13-14/15.15*12-14+13/13-12를 최종적으로 
	12,+, 13,-, 14,/, 15.15,*,12,-, 14,+, 13,/, 13,-, 12의 형태로 배열에 저장
    - 값을 저장할 때 float의 형태로 저장하기 (소수점이 있을 수 있기 때문)
    - 숫자를 따로 분리, 연산자도 따로 저장 후 배열을 합치기.
      - 가장 처음이 연산자면 앞에 0을 추가하기
      - 숫자, 연산자, 숫자의 형태로 합친 뒤 새로운 배열에 저장하기
      - 계산할 때는 곱셈과 나눗셈 먼저 하며 인덱스 번호가 낮은 순서대로 진행한다.
      - 계산을 할 때 
	>`${연산자 앞쪽의 인덱스(숫자)} + ${인덱스 값{연산자(곱셈 또는 나눗셈)} + ${연산자 뒤 쪽의 인덱스(숫자)}
  의 형태로 만들고 계산한 값을 앞쪽에 저장한다.
  계산 파트
  calcIndex-1, calcIndex, calcIndex+1에 해당하는 값을 배열에서 제거한다.
  calcIndex-1의 위치에 결과값을 입력한다.
  반복한다?


## 21.11.10
#### 주요 내용
1. CSS
  - #result의 텍스트 중앙정렬
2. JS
  - 기능부분은 keydown에, 입력부분은 keypress 부분으로 분리.
  > esc와 backspace?는 keypress 값이 없음.
  - 키보드 입력에서 ESC키 눌렀을 때 AC가 동작하도록 설정.
  - 키보드 숫자 입력용 Hash와 연산자 입력용 Hash를 분리.
  - 계산 및 출력파트 완성.
  > (new Function(`return + ${값}`))();
  의 형태로 완성
  - 마우스로 확인("=")버튼 눌렀을 때의 동작 설정
#### 해야할 일
1. JS
  - 마우스로 입력하는 연산자에 대해서 operFlag 적용하기
## 21.11.09
#### 주요 내용
1. babel 추가
  > 프로젝트 폴더에서 npm init -y
  > npm install --save-dev @babel/core @babel/cli

2. node_modules 제외
  > 나중에 git clone했을 때, npm install 을 해주면 package.json을 참조하여 필요한 package를 install 한다.

3. 바벨 사용법
  - js파일에서 npx babel 파일명.js -o 파일명-compiled.js 와 같은 이름으로 생성.
  - js파일에서 npx babel 파일명.js -w -o 파일명-~.js로 watch 할 수도 있음.
  [터미널] JS파일이 있는 디렉토리에서 명령어 입력
  > npx babel calc.js -w -o calc-compiled.js
  를 입력하면 자동으로 변환된다.

4. HTML
  - key클래스의 문자들을 모두 특수문자 처리함.
  - script src를 /body 바로 위로 올림.
  - 완료.
2. CSS
  - 확인 버튼("=")에 모서리 둥글게 깎기
3. JS
  - keyCode별 값을 객체형태의 배열로 선언
  - 변수 선언
  - 로딩시 기본값으로 초기화
  - AC버튼 클릭시 전체 초기화
  - BackSpace 버튼 입력시 가장 마지막으로 입력된 값 지우기
  - 키보드 입력 확인
  - 마우스 입력 확인
----------
#### 해야할 일
1. CSS
  - #result에 출력되는 텍스트 위치 최적화 하기
    - 텍스트가 중앙에 정렬될 수 있도록 변경하기.
2. JS
  - 입력값에서 Flag 적용해서 연산자가 연속적으로 입력되지 않도록 만들기
    - 키보드와 마우스 둘 다 적용될 수 있도록 만들기
  - 확인버튼 만들기
    - "="버튼 클릭과 Enter를 눌렀을 때 동일하게
    - 확인버튼이 클릭되면 숫자가 추가적으로 입력되지 않아야 하며, backspace 또한 동작하지 않아야 한다.
    - CLEAR만 가능하도록 만들어야 할 것 같다.
  - 확인 했을 때, 입력받은 값(#result에 저장된 값)을 파싱하여 result에 출력하기
  - #result에 저장된 값을 이용해서 판매금액, 수수료, 예상 적립금액 계산 후 출력하기.

----------
## 21.11.08 -2
1. html 구조 변경 및 기본적인 HTML 완성
 - 수수료와 계산기를 분리.
2. 기본적인 CSS 스타일 완성
 - 전체적인 위치잡기 완료
 - 기본적인 버튼 완료
 - 눌렀을 때와 호버시 적용할 클래스 생성 완료.
3. README.md 파일 생성
4. TEST용 html, css, js 파일 생성
5. calc.css 파일 무시 (gitignore)

해야 할 일
#### CSS ####
1. 레이아웃용 크기지정 부분과 스타일 지정용 세부 구간이 섞이지 않았는지 확인
#### JS ####
---------제작 순서------------
1. 변수 선언
2. 초기화가 필요한 값들 전부 초기화하기
3. 버튼을 클릭 = 값을 읽어오기
  3-1 숫자일 때는 정상적으로 입력
  3-2 첫 입력이 연산자일 때는 입력 안됌 
    - 연산자가 두 번 연달아 입력되면 마지막 연산자로 교체
      - 먼저 입력된 연산자를 빼고, 새로 입력된 연산자를 넣기.
  - flag 이용하기 기본값 false 숫자 입력시 true
4. 읽어온 값을 #result에 출력하고 연산용 배열이나 어딘가에 저장.
5. 연산자가 한 번 입력되면 flag(위에서 사용한)를 false로 바꾸기
6. 사용자가 엔터를 누르면 결과값 출력하기
 - 마지막으로 입력된 값이 연산자일 경우 (flag가 false 일 경우) 동작하지 않기.
 - 마지막으로 입력된 값이 숫자일 경우 (flag가 true일 경우) 실행
7. AC 가 입력될경우 배열?과 #result의 값 전부 초기화하기
8. back 가 입력될 경우 배열과 #result의 값 중에서 뒤에 있는 값 지우기.

* 분리
  <!-- 입력받는 부분은 전부 상단에 모아놓기 -->
  - 키보드나 마우스가 입력되었을 때 값을 읽어오는 것
    - 입력된 값은 숫자에만 한정될 수 있도록 해야 함.
    - 숫자와 연산자를 텐키리스일 때 어떻게 해야하는지가 문제점.
    - 키보드로 입력하는 것과 마우스로 입력하는 것을 분리하고 저장만 같은곳에 하면 될 것 같음.
  - CLEAR를 누르면 전부 초기화 할 수 있도록 하기
  - BACKSPACE를 누르면 가장 마지막에 입력된 값이 초기화 될 수 있도록 하기
    - BACKSPACE와 연동
    - #result만 뒤에서 한 개 빼기
  <!-- - 전체적인 값, 동작 순서를 한 군데 묶는 것 -->
  - 읽어온 값을 저장
    - 입력되었을 때 return으로 호출될 수 있도록 만들기.
    - 읽어온 값에서 연산자는 flag로 분리
      - 연산자가 2번 입력될 경우 먼저 입력된 값을 지우고 새로운 값으로 입력
      - 숫자가 입력될 경우 flag를 초기화
  - 저장한 값을 #result에 출력
  - "=" 버튼을 눌렀을 때 동작
    - 저장된 값을 계산
      - 계산을 어떻게 할 것인지는 여러가지를 참고해봐야 할 필요가 있음.
      - 계산된 값을 #result에 출력
#### 고민사항
1. 금액의 크기가 클 때 어떻게 할 것인지 생각하기.
 > 현재 54자리는 거뜬하게 출력되는것을 확인.
 > 일정 길이일 때 말줄임표를 사용하거나 소수점이 길어질 때를 대비하는게 필요할 것 같음.
2. #result에서 결과값을 한 줄일때는 중앙에, 여러 줄일때는 위 아래로 늘어날 수 있도록 만들기
## 21.11.08 -1

* html, css, js 파일 생성